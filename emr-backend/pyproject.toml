[tool.poetry]
name = "emr-cluster-manager"
version = "0.1.0"
description = "EMR Cluster Manager Flask Application"
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = "^3.9"
flask = "^2.0.0"
flask-cors = "^3.0.10"
boto3 = "^1.24.0"
waitress = "^2.1.2"
python-dotenv = "^1.0.0"

[tool.poetry.dev-dependencies]
pytest = "^7.0.0"
ruff = "^0.0.262"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"