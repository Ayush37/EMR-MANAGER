[tool.poetry]
name = "emr-serverless-viewer"
version = "0.1.0"
description = "EMR Serverless Log Viewer Flask Application"
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = "^3.9"
flask = "^2.2.3"
flask-cors = "^3.0.10"
boto3 = "^1.24.0"
python-dotenv = "^1.0.0"
waitress = "^2.1.2"
Werkzeug = "~2.3.7"
toml = "^0.10.2"

[tool.poetry.dev-dependencies]
pytest = "^7.0.0"
ruff = "^0.0.262"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"